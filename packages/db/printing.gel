module printing {
    scalar type Material extending enum<
        PLA,
        TPU,
        PETG,
    >;

    scalar type QueueType extending enum<
        PLA,
        PETG,
        TPU,
        `MULTI`
    >;

    scalar type Priority extending enum<
        LOW,
        MEDIUM,
        HIGH,
    >;

    abstract type PrinterStatus;  # Discriminated union
    module printer_status {
        type Disabled extending printing::PrinterStatus {
            end_time: datetime;
        }

        type Disconnected extending printing::PrinterStatus;

        type Idle extending printing::PrinterStatus;

        type Paused extending printing::PrinterStatus;

        type Printing extending printing::PrinterStatus {
            required print: printing::Print;
        }

        type Finished extending printing::PrinterStatus {
            required print: printing::Print;
        }

        scalar type FailureReason extending enum<
            # Main Controller & Electronics
            MAIN_CONTROLLER_BOARD,
            POWER_SUPPLY,
            DISPLAY_BOARD,
            WIFI_MODULE,

            # Temperature Sensors & Heating
            HOTEND_THERMISTOR,
            HEATBED_THERMISTOR,
            HOTEND_HEATER_CARTRIDGE,
            HEATBED_HEATING_ELEMENT,
            HEATER_BLOCK,

            # Motors & Motion System,
            EXTRUDER_MOTOR,
            X_AXIS_MOTOR,
            Y_AXIS_MOTOR,
            Z_AXIS_MOTOR,
            LINEAR_RAILS,
            LINEAR_BEARINGS,
            BELT_SYSTEM,
            PULLEYS,
            LEAD_SCREW_NUT,

            # Cooling System
            HOTEND_FAN,
            PART_COOLING_FAN,
            CHAMBER_FAN,
            POWER_SUPPLY_FAN,

            # Extruder & Hotend
            NOZZLE,
            HEAT_BREAK,
            HEAT_SINK,
            EXTRUDER_GEARS,
            BOWDEN_TUBE,

            # Sensors & Detection
            FILAMENT_SENSOR,
            BED_LEVELING_SENSOR,
            DOOR_SENSOR,
            CRASH_DETECTION_SENSOR,
            POWER_PANIC_SENSOR,

            # Print Bed & Levelling
            PRINT_BED_SURFACE,
            BED_LEVELLING_SPRINGS,
            BED_MOUNTING_HARDWARE,

            # Wiring & Connections
            HOTEND_WIRING,
            HEATBED_WIRING,
            MOTOR_WIRING,
            MAIN_POWER_CABLE,
            USB_CONNECTION,

            # Structural Components
            FRAME_COMPONENTS,
            SMOOTH_RODS,
            ENCLOSURE_PANELS,
            FILAMENT_FEEDING,

            OTHER,
        >;

        type Failed extending printing::PrinterStatus {
            required reason: FailureReason;
            required note: str;
        }
    }

    type Printer {
        required name: str;
        required ip: str;
        required keys: array<str>;
        required manufacturer: str;
        required model: str;
        required has_camera: bool;
        required status: PrinterStatus;
        required filament_slots: array<tuple<
            material: Material,
            colour: str,
            nozzle_temp_min: int16,
            nozzle_temp_max: int16,
            bed_temp: int16,
        >>;
        required location: sign_in::Location;
        required total_print_mass: float32; #2dp like print job
        required total_print_time: duration;
        multi prints := (
            select Print filter __source__ in .`on`.printer
        );
        multi downtimes := .<printer[is Downtime];
    }

    type Downtime extending default::CreatedAt {
        required printer: Printer {
            on target delete delete source;
        }
        required start_time: datetime;
        end_time: datetime;
        required has_started: bool {
            default := false;
        }
        required has_finished: bool {
            default := false;
        }
        reason: str;

        constraint expression on (.end_time > .start_time);
    }

    function cdn_url(id: uuid, path: str) -> str {
        using (
            with
                root := assert_exists(global default::CDN_URL, message := "CDN_URL is not set"),
            select "\(root)/\(path)/\(id)"
        )
    }

    type Print {
        required name: str;
        required filament: array<tuple<
            material: Material,
            colour: str,
            nozzle_temp_min: int16,
            nozzle_temp_max: int16,
            bed_temp: int16,
        >>;
        required duration: duration;
        required mass: float32;  # only 2dps
        required author: users::User;
        required approved_by: users::Rep;
        required uploadedAt: datetime;
        required priority: Priority;
        reason: str;
        multi `on`: PrintHistory;
        required gcode_path := "\(cdn_url(.id,'prints')).gcode";
        required threemf_path :=  "\(cdn_url(.id,'prints')).3mf";
    }

    type PrintHistory extending default::CreatedAt {
        required queue: QueueType; # Defines which general queue
        printer: Printer; # Not required as it may be on the general queue for any colour, define when sent to printer
        required status: PrintStatus;
        required attempts: int16 {
            default := 0;
            constraint min_value(0);
        }
        required has_timelapse: bool;
        timelapse_path := "\(cdn_url(.id,'timelapse')).mp4";

        index on (.printer);

        trigger log_insert after insert for each do (
            with print := __new__.<`on`[is Print],
            insert PrintAuditEntry {
                printer := __new__.printer,
                status := __new__.status,
                print := assert_single(assert_exists(print, message := "PrintHistory was not created as a sub-insert")),
            }
        );

        trigger log_update after update for each do (
            with print := __new__.<`on`[is Print],
            insert PrintAuditEntry {
                printer := __new__.printer,
                status := __new__.status,
                print := assert_single(assert_exists(print)),
            }
        );
    }

    abstract type PrintStatus;  # Another discriminated union
    module print_status {
        type Queued extending printing::PrintStatus;

        type UnderReview extending printing::PrintStatus;

        type Printing extending printing::PrintStatus {
            required print: printing::Print;
        }

        type Complete extending printing::PrintStatus;

        type Cancelled extending printing::PrintStatus;

        scalar type FailureReason extending enum<
            NO_EXTRUSION_AT_PRINT_START,
            POOR_BED_ADHESION,
            UNDER_EXTRUSION,
            OVER_EXTRUSION,
            GAPS_IN_TOP_LAYERS,
            STRINGING_AND_OOZING,
            OVERHEATING,
            LAYER_SHIFTING,
            LAYER_SEPARATION_AND_SPLITTING,
            FILAMENT_GRINDING,
            EXTRUDER_CLOG,
            EXTRUSION_STOPS_MID_PRINT,
            WEAK_INFILL,
            BLOBS_AND_ZITS,
            GAPS_BETWEEN_INFILL_AND_PERIMETER,
            CORNER_CURLING_AND_ROUGHNESS,
            TOP_SURFACE_SCARRING,
            CORNER_GAPS_IN_BOTTOM_LAYER,
            LAYER_LINES_ON_SIDES,
            VIBRATION_AND_RINGING,
            THIN_WALL_GAPS,
            SMALL_FEATURE_LOSS,
            INCONSISTENT_EXTRUSION,
            WARPING,
            POOR_OVERHANG_QUALITY,
            DIMENSIONAL_INACCURACY,
            POOR_BRIDGING,
            FILAMENT_FEEDING,
            FILAMENT_RAN_OUT,
            NOT_A_CLUE,
            OTHER,
        >;

        type Failed extending printing::PrintStatus {
            required reason: FailureReason;
            note: str;
        }
    }

    abstract type AuditEntry extending default::CreatedAt {
        required printer: Printer;
        required status: PrinterStatus | PrintStatus;

        index on ((.printer, .status));
    }

    type PrinterAuditEntry extending AuditEntry {
        overloaded required status: PrinterStatus;
    }

    type PrintAuditEntry extending AuditEntry {
        overloaded required status: PrintStatus;
        required print: Print;
    }
}