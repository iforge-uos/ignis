{
  "name": "mine",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "format": "rustfmt src/*.rs --edition=2021",
    "start": "cargo run",
    "dev": "OP_ACCOUNT=iforge.1password.com op run --env-file=.env -- cargo watch -x run",
    "lint": "cargo clippy",
    "lint:fix": "cargo clippy --fix"
  },
  "devDependencies": {
    "@ignis/lint": "workspace:*",
    "eslint": "^8.56.0"
  }
}
